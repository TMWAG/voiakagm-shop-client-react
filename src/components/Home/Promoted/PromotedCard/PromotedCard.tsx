import React from 'react';
import styles from './PromotedCard.module.css';

const PromotedCard = (): JSX.Element => {
  return <div className={styles.promotedCard}>card</div>;
};

export default PromotedCard;
